/**
 * BuilderScreen — Multi-Clip Builder (Phase 9.4.x). Aktuell Coming-Soon Placeholder.
 */

import { ComingSoon } from '../components/ComingSoon';

export function BuilderScreen() {
  return (
    <ComingSoon
      icon="construct-outline"
      title="Builder"
      description="Stitch clips, overlays & transitions into a single export — same engine as Desktop, mobile-shaped."
      phaseTag="Phase 9.4.x"
    />
  );
}
