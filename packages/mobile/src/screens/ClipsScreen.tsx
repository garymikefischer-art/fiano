/**
 * ClipsScreen — Clip-Studio (Phase 9.4.x). Aktuell Coming-Soon Placeholder.
 */

import { ComingSoon } from '../components/ComingSoon';

export function ClipsScreen() {
  return (
    <ComingSoon
      icon="cut-outline"
      title="Clip Studio"
      description="Trim, polish & ship your highlights — coming with the native FFmpeg module in Phase 9.4.x."
      phaseTag="Phase 9.4.x"
    />
  );
}
