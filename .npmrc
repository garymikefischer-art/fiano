# Monorepo mit Desktop (React 18) + Mobile (React 19) → Peer-Konflikte unvermeidbar.
# legacy-peer-deps gilt damit für `npm install` UND `npm ci` (EAS) konsistent,
# sonst erzeugt der CLI-Flag-Install einen Lock, den `npm ci` als out-of-sync ablehnt.
legacy-peer-deps=true
